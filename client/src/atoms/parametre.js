import { atom } from "recoil";

export const table_name = atom({
  key: "table_name-parametre",
  default: "caisse",
});

export const countArr = atom({
  key: "countArray-parametre",
  default: "caisse",
});
